"use strict";

// var names = ['Andrew', 'Julie', 'Jen'];

// names.forEach(name => console.info(`name: ${name}`));

// function add(a, b) {
//   return a + b;
// }

let add = (a, b) => a + b;

console.log(add(1, 3));
console.log(add(9, 0));